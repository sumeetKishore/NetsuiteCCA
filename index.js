'use strict';

/**
 * creates clean code architecture scaffold for netsuite development
 * @param {string} packageName
 * @param {string} scriptType
 * @return {booelean}
 */
module.exports = function(packageName, scriptType) {
    if(packageName,scriptType){
        return true;
    } 
};